from tkinter import *

root = Tk()

#Creating label widget
PRTest = Label(root, text="PrTest Window")

#Placing it onto the gui screen
PRTest.grid(row = 0, column= 0)

#Creates a loop
root.mainloop()